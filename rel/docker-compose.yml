---
version: '2'

services:

  app:
    build:
      context: ".."
      dockerfile: "./rel/Dockerfile.dev"
    command: "mix phoenix.server"
    ports:
      - "4000:4000"
    volumes:
      - "..:/opt/app"
    depends_on:
      - "redis"

  redis:
    image: redis
